using System.Runtime.CompilerServices;

// The info-popup demo timeline, builder and catalog (issue #446) are internal to Presentation; the
// EditMode suite samples them directly to pin timing, easing and loop-wrap behaviour.
[assembly: InternalsVisibleTo("MustyBlockBlast.Tests.EditMode")]
